# RemoteBrowser 多用户与持久浏览器实例需求文档

版本：vNext / 0.2

日期：2026-09-16

状态：用户已确认 v0.2 范围，实施与验收进行中；完成证据见 TEST-COVERAGE.md 与 BROWSER-TEST-REPORT.md。

验收执行调整（2026-09-17）：用户明确将 A-09 中真实24小时等待验证接管，自行完成，要求代理忽略该项等待。产品长期保留要求不变；代理不将约11小时的阶段证据声称为24小时通过，其他实现和测试继续收尾。

## 1. 产品目标与需求来源

将 RemoteBrowser 从以默认管理员、临时会话为主的 MVP，调整为管理员分配账号和实例配额的多用户远程浏览器平台。用户拥有相互隔离、长期保留的浏览器实例，随时登录平台继续使用。

本次用户已明确的要求：

| 编号 | 要求 |
| --- | --- |
| U-01 | 一个浏览器实例对应一个独立的远程浏览器环境。 |
| U-02 | 部署时配置管理员账号及可选的邮件 provider。 |
| U-03 | 管理员通过管理后台新增用户，并为每个用户指定可以创建的浏览器实例数量。 |
| U-04 | 配置邮件 provider 时，通过邮件发送新用户密码；未配置时，由管理员分配密码。 |
| U-05 | 普通用户通过邮箱和密码登录，支持修改自己的密码。 |
| U-06 | 普通用户在配额内创建和使用自己的浏览器实例。 |
| U-07 | 浏览器实例不主动删除就长期保留，不因空闲时间而过期或自动删除。 |
| U-08 | 管理员具有用户列表管理功能，能够查看每个用户创建的浏览器实例数。 |
| U-09 | 管理员能够修改 / 重置普通用户的密码。 |
| U-10 | 管理员能够删除普通用户，同时删除其名下全部浏览器实例和数据；操作前明确二次确认。 |

下文将这些要求展开为可实现、可验收的规则。建议默认值集中列在第 12 节，已随本文整体获得实施确认。用户后续明确本机为可清理的测试环境且无需备份；该授权只调整本机部署操作，不取消产品的旧数据迁移兼容要求。

### 1.1 为什么取消空闲超时

现有实现会在连接全部断开后，将会话标为 `disconnected`，超过 `RB_SESSION_IDLE_TIMEOUT` 后标为 `expired` 并停止容器。它的作用是回收无人使用的临时会话所占的运行资源。当前代码并不会在这个步骤删除容器和 profile。

新的产品定位要求实例长期存在，因此取消这项空闲回收机制，不再通过“多久没人连接”决定实例寿命。资源使用改由每用户实例数量、单实例资源限制以及用户手动启停控制。

### 1.2 与旧文档的关系

本文是本次重设计的专项需求草案，保留原有 `PRD.md` 和 `TECH-SPEC.md` 作为历史基线。确认后，应同步修订旧文档中的单用户、空闲过期、关闭即清空、持久化仅为后续功能等冲突描述。两类固定角色属于本次范围，不扩展为部门、审批或自定义权限体系。

## 2. 核心概念与生命周期原则

| 概念 | 定义 |
| --- | --- |
| 用户 | 登录平台的账号，包含身份、角色、密码和个人实例数量上限。 |
| 浏览器实例 | 归属于一个用户的长期资源，拥有固定 ID、独立浏览器 profile、下载目录和运行环境。 |
| 容器 | 承载实例的运行载体。容器停止或因维护被替换，不等于实例被删除。 |
| 访问连接 | 本地浏览器到远程实例的控制、画面、声音等连接；可随时断开和重连。 |
| 平台登录会话 | 用户登录后的身份凭证，可以退出、失效或被撤销，不决定浏览器实例的寿命。 |

“一直在”的建议完整语义：创建成功后默认持续运行；关闭访问页面、退出平台、登录失效、长时间不操作都不触发停止或删除。用户可以主动停止以节省运行资源，之后再启动同一个实例。

长期保留包括已落盘的浏览器配置、Cookie、书签、历史记录、站点本地存储和下载文件。正常断开重连时继续使用原浏览器进程和标签页。容器或主机重启后恢复持久数据，但不承诺恢复未保存的网页内容或进程内存，也不承诺第三方网站登录永不过期。

## 3. 角色与权限

本期采用 `admin`（管理员）和 `user`（普通用户）两种固定角色。普通用户不开放自助注册，由管理员创建。

| 能力 | 管理员 | 普通用户 |
| --- | --- | --- |
| 登录、退出、修改本人密码 | 支持 | 支持 |
| 新增用户、查看用户列表及每个用户的实例数 | 支持，本期必需 | 不支持 |
| 修改用户实例配额 | 支持 | 不支持 |
| 修改 / 重置普通用户密码 | 支持，本期必需，无需该用户原密码 | 不支持 |
| 删除普通用户及其全部实例和数据 | 支持，本期必需；执行第 5.8 节的二次确认和完整清理规则 | 不支持 |
| 重发开户通知 | 支持，建议纳入本期 | 不支持 |
| 禁用、启用普通用户 | 支持，建议纳入本期 | 不支持 |
| 查看本人实例、创建、进入、启停、删除 | 支持，遵守自己的配额 | 支持，遵守自己的配额 |
| 查看全站用户实例数量、运行状态等管理信息 | 支持 | 不支持 |
| 进入其他用户桌面、读取其文件或剪贴板 | 本期不提供 | 不支持 |
| 单独停止或删除其他用户实例 | 尚未明确，不默认开放；删除用户时具有清理该用户全部实例的权限 | 不支持 |

所有权限必须由服务端检查，覆盖页面、接口、文件、剪贴板、noVNC、WebRTC、音频和输入通道，不能仅靠隐藏按钮。实例 ID 或访问地址本身不构成授权。

这里约定的是平台功能边界；持有宿主机、Docker 或数据库访问权限的部署运维人员，仍有底层访问能力。

## 4. 部署与管理员初始化

### 4.1 首次部署

部署者应配置管理员初始凭据、数据持久化目录、独立的登录签名密钥和平台对外访问地址；邮件 provider 为可选配置。

建议保留内置管理员登录名 `admin`，部署时设置初始密码；普通用户统一使用邮箱。登录表单显示“邮箱 / 管理员账号”。管理员可选绑定通知邮箱。该兼容方案可在评审时调整为管理员也必须使用邮箱。

首次启动没有管理员时创建管理员；管理员已存在时不重复创建。初始配置只用于初始化，不在每次重启时覆盖数据库里的账号密码。部署文档、配置说明和启动日志必须明确这一点，避免再次出现“环境变量密码与实际登录密码不同”的误解；日志不得打印密码。

管理员后续通过修改密码功能更改密码。管理员遗忘密码时，应提供部署运维人员可显式执行的重置方式，不依赖删除数据库，也不隐式依赖修改环境变量。

### 4.2 配置要求

| 配置项 | 要求 |
| --- | --- |
| 管理员初始密码 | 首次初始化时必须明确配置；不得依赖通用默认密码。 |
| 管理员邮箱 | 建议可选，用于通知；不影响没有邮件服务时的部署。 |
| 平台访问地址 | 发送开户邮件时必需，用于生成用户可访问的登录链接。 |
| 登录签名密钥 | 独立于管理员密码并持久配置，改密码不应偶然影响其他账号登录。 |
| 邮件 provider | 可不配置；建议首期支持 SMTP，预留其他 provider 扩展能力。 |
| 默认实例数量上限 | 管理员新增用户时的表单默认值，最终配额保存到每个用户。 |
| 单实例 CPU、内存、共享内存限制 | 保留现有部署配置，不将“永久存在”解释为资源无限。 |

若采用 SMTP，部署配置需包含服务器地址、端口、认证信息、发件人和传输加密方式，并支持测试发送。完全未配置与已配置但不可用应明确区分；配置不完整或测试失败须显示具体问题，不能假报邮件功能可用。

移除 `RB_SESSION_IDLE_TIMEOUT` 的生命周期作用；升级时如果仍配置此变量，应给出已弃用提示并忽略，不能继续触发旧回收行为。现有 `RB_MAX_SESSIONS` 在迁移后不再覆盖所有用户的个人配额，其兼容方式见第 10 节。具体新增变量名留到技术设计阶段确定。

## 5. 用户管理与密码流程

### 5.1 管理员新增用户

新增用户表单至少包含邮箱、实例数量上限，以及无邮件服务时的初始密码。显示名称可选，角色固定为普通用户。

规则：

- 邮箱必填、格式有效且全站唯一；建议按统一大小写与去除首尾空白后的值查重和登录，不合并加号别名或点号变体。
- 实例配额是大于等于 0 的整数，不能省略成“无限”。配额为 0 允许登录，但不能创建实例。
- 用户创建成功后立即出现在管理列表中，显示配额和密码交付结果。
- 建议初始密码首次登录后必须修改；完成前仅开放修改密码和退出操作。

### 5.2 配置邮件 provider 时

管理员提交邮箱和配额后，系统生成随机初始密码并创建账号，向该邮箱发送登录地址、账号、初始密码和首次修改密码说明。保留用户提出的“邮件发送密码”方式；发送的是初始临时密码，不发送用户日后自行设置的密码。

邮件交付与账号创建应分别展示结果：

- provider 接受请求后显示“邮件已提交发送”，不能保证收件人已经收到或阅读。
- 发送失败时保留用户记录并显示失败原因，不假报成功，也不因为重复点击而重复创建账号。
- 管理员可重试通知；涉及密码重发时生成新的初始密码并使旧值失效，明确告知管理员该操作会重置目标账号的密码并撤销其旧登录凭据。已完成首次改密的用户应走明确的“重置密码”流程，不能伪装成无影响的通知重发。
- provider 故障时，可显式选择“管理员设置临时密码并自行交付”，不能静默切换交付方式。
- 不在普通日志、账号列表或长期明文存储中保留初始密码、邮件认证密钥。后续无法查回用户原密码，只能重置。

### 5.3 未配置邮件 provider 时

管理员新增用户时必须设置初始密码，并自行交付给用户。表单明确显示“未配置邮件服务，密码需要自行交付”，系统不尝试发送邮件。用户之后按相同的登录和修改密码流程使用平台。

### 5.4 普通用户登录与修改密码

用户输入邮箱和密码登录；验证通过后进入“我的浏览器实例”。初始密码待修改的账号先进入改密页面。

修改密码需输入当前密码、新密码和确认新密码。建议本期密码最少 12 个字符，并在所有设置入口使用一致校验。密码只能以适合密码校验的哈希形式保存。

建议改密成功后撤销旧登录凭据，并要求重新登录。初始密码和管理员重置密码也使用相同的凭据撤销机制。登录失败提示不暴露账号是否存在，登录和重置入口有请求频率限制。

账号忘记密码时，本期通过管理员重置解决，具体流程见第 5.7 节。自助“忘记密码”邮件链接流程列为后续功能。

### 5.5 账号禁用与恢复（建议纳入本期）

禁用后禁止新登录，并撤销该用户的现有登录和远程访问连接；建议在操作完成后 10 秒内生效，包括已建立的 WebRTC / WebSocket 通道。实例、profile 和下载文件保留，已经运行的实例不因账号禁用被自动删除或停止。

重新启用后用户可以重新登录并使用原实例。禁用与删除是两个不同操作；管理员删除用户属于本期必需功能，规则见第 5.8 节。

退出或撤销登录只影响访问权限，浏览器进程仍可继续运行。登录有效期建议沿用当前 7 天；登录到期后重新认证，不生成新的浏览器实例。

### 5.6 管理员用户列表（本期必需）

管理后台提供独立的“用户管理”入口，集中呈现账号信息、实例数量和管理操作。

| 列表字段 | 含义 |
| --- | --- |
| 邮箱 / 管理员账号 | 用户的登录标识；显示名称如有则一并展示。 |
| 角色与账号状态 | 区分管理员、普通用户，以及正常、禁用、删除处理中等状态。 |
| 已创建实例数 | 当前归属于该用户且尚未完成删除的实例数量，与配额占用一致；包含停止、异常和创建中的实例。 |
| 实例数量上限 | 管理员为该用户配置的配额，可显示为“已创建 2 / 上限 5”。 |
| 创建时间 | 用户账号创建时间。 |
| 密码交付状态 | 显示手工交付、邮件已提交发送或发送失败；不显示用户密码。 |
| 操作 | 修改配额、修改 / 重置密码、删除用户；启用 / 禁用、重发通知按对应建议功能提供。 |

新增用户入口位于列表页。建议支持按邮箱 / 显示名称搜索、按账号状态筛选以及分页，避免用户增加后难以定位。

实例计数必须来自服务端真实的用户归属数据，不能只数正在运行的容器；创建占位或删除完成后刷新计数。这里的“已创建实例数”表示当前拥有数量，不是账号自创建以来的累计创建次数，累计统计本期不要求。

建议点击实例数可查看该用户的实例名称、ID、运行状态、创建时间和最后访问时间。此管理信息视图不自动授予进入桌面、读取文件或操作剪贴板的权限。

### 5.7 管理员修改 / 重置用户密码（本期必需）

管理员从用户列表选择目标普通用户，可以直接填写新密码及确认密码，不需要知道该用户的旧密码。也可提供“生成随机临时密码”选项。新密码沿用平台统一的密码规则，旧密码不提供明文查看或找回。

配置邮件 provider 时，默认将新临时密码发送给目标用户，并单独展示投递结果；未配置时由管理员自行交付。邮件配置不限制管理员手工设置新密码的能力，投递失败遵循第 5.2 节的显式手工交付与重试规则。

提交前展示目标邮箱及“旧密码、旧登录凭据将失效”的说明。修改成功后撤销该用户现有登录和远程访问权限，建议按第 5.5 节的 10 秒上限生效；用户下次登录按首次改密规则处理。修改密码不停止、删除或新建任何浏览器实例，不影响其他用户。

仅允许管理员调用此功能，普通用户只能通过需要本人当前密码的个人改密入口修改自己的密码。建议记录管理员、目标用户、时间和操作结果，不记录明文密码。

### 5.8 管理员删除用户（本期必需）

管理员可以从用户列表删除普通用户。用户已明确选择：**删除账号的同时，删除其名下全部浏览器实例及数据，操作前二次确认。** 删除完成后，该账号不能登录，旧登录凭据和远程访问权限失效，也不能继续创建浏览器实例。此功能取代上一版草案“本期不提供用户硬删除”的限制。

通用处理规则：

- 操作前明确显示目标邮箱、当前实例数量，并说明将永久删除所有运行中、停止、异常等状态的实例、浏览器 profile 和下载文件；管理员须再次确认才执行，取消确认不产生任何删除。
- 建议保护内置 `admin` 和管理员本人，不允许通过此入口删除，避免平台失去管理入口。
- 开始实际删除处理后将账号标为“删除中”，阻止目标用户的新登录、新建 / 启动实例、改密和文件写入等操作，并撤销现有登录和远程访问权限。处理已在进行的创建或后台恢复任务，避免清理之后又生成实例或重新启动容器。
- 删除失败时保留可追踪的用户及资源关联，显示“删除失败 / 待处理”并允许管理员重试；处理中断或重复提交不得遗留孤儿实例，也不得影响其他用户。
- 已删除邮箱可以用于新建账号，但新账号获得新的用户 ID，不自动继承原账号的实例或权限。

执行顺序：

1. 确认用户身份和最新实例归属，将账号置为删除中并冻结上述操作。
2. 收敛并发中的实例创建、启动、恢复和文件写入，停止该用户运行中的容器，清理全部所属实例的容器、profile、下载文件及相关资源。
3. 每个实例确认完整清理后才记为删除完成；保留未完成项与失败原因，支持幂等重试和服务重启后继续处理。
4. 所有资源均确认清理完成后，才完成用户账号删除并从正常用户列表移除；管理操作记录如启用则保留脱敏结果。

容器不存在不能直接证明数据已删除，Docker 不可达也不能当作资源不存在。部分清理失败时账号保持不可登录，并在管理列表中展示进度；不能先移除账号及实例归属记录后再盲目清理。不自动将数据转交其他用户，也不凭容器名称相似而清理未能核实归属的资源。

账号删除属于管理员明确确认的资源销毁，与空闲时间无关。除用户本人删除实例或管理员确认删除整个用户外，实例继续遵守长期保留原则。

## 6. 每用户实例配额

配额定义为“一个用户同时拥有的、尚未完成删除的实例总数上限”，不是累计创建次数，也不是当前在线连接数或正在运行的容器数。

| 实例情况 | 是否占配额 |
| --- | --- |
| 创建中、启动中、运行中 | 占用 |
| 无访问连接、手动停止、异常、容器缺失但保留实例记录 | 占用 |
| 删除中、删除失败且尚有待处理资源 | 占用 |
| 已完整删除 | 不占用 |
| 创建请求被拒绝且未形成任何实例资源 | 不占用 |

例如配额为 3，已有两个运行实例和一个停止实例，仍显示“已用 3 / 上限 3”，不能再创建。删除一个完成后才能再创建。

删除用户期间，列表中的实例数按尚未完成清理的实例计算并展示清理进度；账号保持冻结，不因部分实例清理完成而允许再次创建。

创建时服务端必须原子地检查和占用名额；两个页面同时提交也不能突破上限。管理员调整配额与用户创建并发发生时，结果也要遵循同一规则。重复提交同一个创建操作不能额外创建实例或重复扣减名额。

创建中途失败如留下实例或数据，应显示可重试、可删除的异常实例并继续占位；只有确认完整回滚、没有留下资源时才释放名额，不能隐藏残留资源。

管理员提高配额后立即生效。降低到现有实例数以下时保留全部现有实例，禁止新增，直到已拥有数量小于新上限。例如已有 3 个实例、配额降为 1，不强行删除或停止任何实例。

达到配额与宿主机资源不足是不同错误，界面应分别解释；容量不足不能触发淘汰旧实例。首期不增加计费套餐、租期或自动扩容。

## 7. 持久浏览器实例管理

### 7.1 创建与使用

建议用户可为实例填写名称，系统生成固定 ID。创建时分配独立运行环境、profile 和下载目录，归属用户不可由普通用户修改。创建成功后默认启动并可进入使用。

同一实例可从新的本地浏览器页面重连，不消耗新名额、不生成新 profile。多个访问连接操作的是同一个远程浏览器，不是额外实例。不同实例之间不共享 profile、Cookie 或下载目录；不承诺各实例有独立公网 IP 或不同硬件指纹。

### 7.2 生命周期动作

| 触发事件 | 预期行为 |
| --- | --- |
| 关闭访问标签页、本地网络断开 | 仅断开访问连接，远程实例继续运行。 |
| 退出平台、平台登录到期、用户改密 | 撤销相应访问权限，实例及数据保留，运行状态不因登录变化而改变。 |
| 30 分钟、一天或更久没有连接 | 无生命周期变化，无过期或空闲自动停止。 |
| 用户主动停止 | 确认容器停止后显示“已停止”，保留实例、profile 和下载文件，仍占名额。 |
| 用户再次启动 | 启动原实例，继续使用原持久数据，不新增实例或扣减额外名额。 |
| 容器异常退出 | 保留实例和数据，尝试有限次数恢复；失败显示异常原因，不能当作删除。 |
| manager 重启 | 恢复管理状态并核对已有实例，不因无连接记录而删除或判定过期。 |
| Docker 或宿主机重启 | 基础设施恢复后，使原本应运行的实例恢复运行；用户手动停止的实例保持停止。 |
| 用户确认删除 | 停止运行环境、移除容器和对应数据，确认完成后释放配额。 |
| 管理员二次确认删除用户 | 删除该用户全部实例的容器、profile、下载文件及相关数据；全部清理完成后删除账号。 |

持续运行依赖宿主机、Docker 和存储可用；实例持久化不等于在主机断电期间仍能运行，也不替代备份。异常恢复不得用空白 profile 静默替换原数据。

### 7.3 状态展示

将实例运行状态与访问连接状态分开展示，避免把“用户已断开”理解成“实例已停止”。

| 运行状态 | 用户看到的含义 |
| --- | --- |
| 创建中 / 启动中 | 正在准备运行环境。 |
| 运行中 | 浏览器环境可使用；同时标注“有连接”或“无连接”。 |
| 停止中 / 已停止 | 正在执行停止，或已确认停止；数据仍保留。 |
| 删除中 | 正在销毁该实例的资源。 |
| 异常 | 创建、运行、停止、恢复或删除失败，说明具体操作及可采取的动作。 |
| 状态待确认 | 暂时无法联系 Docker 或核对实际状态，不能假报为已停止、已删除。 |

移除正常生命周期中的 `expired`。最后访问时间用于展示，不作为回收依据。技术实现可进一步拆分操作状态、实际状态和期望状态，但页面只展示用户需要判断的结果。

### 7.4 删除与故障一致性

用户主动删除实例、管理员二次确认删除用户，是本期销毁实例的两个明确入口。单个实例删除提示应包含实例名称和数据清理说明；删除用户提示应包含目标邮箱、实例总数及全部浏览器数据和下载文件将被删除的说明。平台首期不提供回收站。

只有服务端确认属于该实例的容器和数据清理成功，才显示删除完成并释放配额。停止、删除失败必须可见并支持重试，不能先删除管理记录后把仍运行的容器遗留在后台。操作重试不得误删其他实例。

删除用户必须复用相同的实例清理规则，不能仅通过数据库级联删除记录来替代实际容器与文件清理。账号在资源全部清理完成前保留可追踪的归属与任务状态。

manager 启动和运行期间应核对实例记录与容器状态。对没有数据库记录的旧容器只报告为待处理资源，不自动删除、不猜测所属用户。新容器应带可核验的部署、用户与实例归属标识。

## 8. 页面与交互范围

以下是页面组织建议，可合并为同一后台的不同区域。资源概览、邮件状态与测试属于补充建议，不作为用户已明确提出的独立页面要求。

| 页面 | 最小内容 |
| --- | --- |
| 登录页 | 邮箱 / 管理员账号、密码、清晰的登录错误提示。 |
| 首次改密 / 个人设置 | 当前密码、新密码、确认密码；初始密码用户须先完成改密。 |
| 我的浏览器实例 | 名称、状态、连接情况、创建时间、最后访问时间、配额占用；创建、进入、启停、删除操作。 |
| 远程浏览器页 | 延续现有画面、音频、输入、剪贴板和文件功能；提供返回列表、断开连接入口。 |
| 管理员用户列表（必需） | 邮箱、角色、账号状态、已创建实例数 / 配额、账号创建时间和密码交付状态；新增、修改配额、修改 / 重置密码、删除用户及全部实例；删除前二次确认，删除中展示进度与失败重试；建议增加搜索筛选、分页、实例信息详情及启用 / 禁用。 |
| 管理员资源概览 | 实例归属、运行状态、配额占用、异常和待处理容器信息，不提供其他用户浏览内容的预览。 |
| 邮件配置状态 | 是否启用、provider 类型、脱敏发件信息和测试发送结果；凭据从部署配置读取。 |

面向用户将“会话管理”统一更名为“浏览器实例”，普通用户首页使用“我的浏览器实例”。不必仅为改名强制修改现有底层表名或全部路由。

界面不再显示含糊的“会话过期”：登录凭据失效应提示“登录已过期，请重新登录”，不能使用户误认为浏览器数据已被销毁。时间按用户本地时区显示，并让详情可辨识时区。

## 9. 数据与基本质量要求

实现需具备以下数据能力，具体表结构在技术设计阶段确定：

- 用户：稳定 ID、规范化邮箱、角色、账号状态、密码哈希、个人配额、首次改密标记、凭据撤销信息、创建及更新时间；删除处理应可追踪进度和失败原因。实例计数由所属实例汇总，不能与配额使用数各用不同口径。
- 实例：稳定 ID、所属用户、名称、实际及期望运行状态、容器关联、profile 与下载目录、创建与访问时间、失败原因。
- 邮件通知：收件账号、通知类型、交付状态、脱敏错误、发送时间；不将完整密码邮件正文写入长期日志。
- 管理操作记录（建议）：添加 / 禁用 / 删除用户、改配额、修改 / 重置密码、启停 / 删除实例等操作的操作者、目标和结果。仅用于管理追踪，不采集浏览内容、键盘输入或密码。

登录后权限检查、跨用户访问拒绝、账号禁用和密码重置撤销不能只在页面加载时发生。修改状态的请求应具备必要的 CSRF 防护；持久化目录、文件路径和代理通道须遵守相同的用户边界。

邮件 provider 未配置或暂不可用，不得影响已有用户使用有效密码登录、改密和操作已有实例。持久数据应独立于可替换的容器文件系统，部署升级不得隐式清空用户数据。

## 10. 从当前版本迁移

迁移是后续实施的一部分，本草案不执行迁移。

1. 迁移前备份账号、实例元数据、profile 和下载目录，形成可回退的版本边界。
2. 将现有 `admin` 用户明确标记为管理员，保留原用户 ID、已有密码哈希和实例归属，不重新创建同名账号覆盖数据。
3. 现有用户的初始实例配额从当前配置值迁移；若已拥有数量超过配额，保留实例并应用“禁止新增”规则。新用户配额由管理员明确指定。
4. 移除空闲回收器。已有 `disconnected` 会话依据容器实际情况映射为“运行中，无连接”或对应异常状态。
5. 旧 `expired` 记录仍视为存在的实例：容器停止且数据完整的转换为“已停止”，提供启动入口；容器实际运行的映射为运行中；数据或容器异常的保留记录并说明原因，不清空重建。
6. 对缺少记录或无法核实归属的旧容器列出待处理清单；不自动认领、停止或删除。
7. 将旧登录凭据失效并要求重新登录，以加载新权限规则；不因此销毁或重建浏览器实例。

当前需要变更的主要能力如下，实际代码结构留到技术设计：

| 当前实现 | 目标 |
| --- | --- |
| 默认 `admin`，用户模型只有用户名和密码等基础信息 | 增加邮箱、固定角色、账号状态和个人配额。 |
| `RB_MAX_SESSIONS` 的同一上限用于按用户统计活跃会话 | 每用户独立上限，统计全部未删除实例并防并发超额。 |
| 断开后空闲回收为 `expired` | 无空闲回收，访问连接与运行状态独立。 |
| 已有实例归属检查 | 保留并覆盖多用户、角色限制及长连接撤销。 |
| 缺少用户管理、改密和邮件交付功能 | 增加用户列表、实例数量展示、配额管理、管理员改密及用户删除，并提供个人改密和可选邮件流程。 |
| 部分停止 / 删除错误被忽略 | 确认实际结果后更新状态，失败可见、可重试。 |

## 11. 验收标准

以下为目标版本的验收要求，不代表当前版本已通过。

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| A-01 | 首次部署与重复启动 | 仅创建一个管理员；重启不覆盖已有密码，重置有明确入口。 |
| A-02 | 无邮件 provider 新增用户 | 必须填写初始密码；邮箱登录成功，可修改密码，全流程不依赖邮件。 |
| A-03 | 有邮件 provider 新增用户 | 发往正确邮箱，邮件含可访问登录地址及初始密码；按建议首次登录须改密。 |
| A-04 | 邮件失败与重试 | 账号只创建一次，失败可见；重发 / 重置明确提示目标账号旧密码和旧登录失效；邮件故障不阻断正常密码登录，目标账号的重置不影响其他用户。 |
| A-05 | 身份隔离 | 用户 A 无法通过页面、改 ID、直接 API、文件或任一音视频 / 输入通道访问用户 B 的实例；不能调用管理接口。 |
| A-06 | 配额包含停止实例 | 配额 2，已有 1 运行、1 停止时禁止第三个；完整删除一个后允许新建。 |
| A-07 | 并发创建与失败 | 剩一个名额时同时提交多个请求只成功占一个；中途失败资源可见且可清理，无隐藏容器或配额泄漏。 |
| A-08 | 下调配额 | 已有 3 个、配额降到 1 时，原实例与数据不受影响，不能新增。 |
| A-09 | 长时间断开 | 关闭所有访问连接超过旧 30 分钟阈值，并验证至少 24 小时无人连接，实例仍运行、无过期记录，重连使用同一 ID 和 profile。 |
| A-10 | 平台登录与实例独立 | 退出、模拟登录到期、改密后需要重新登录，浏览器实例和后台运行不因此终止。 |
| A-11 | 持久数据 | 在浏览器中建立书签、站点存储和下载文件；断开重连、手动停止再启动、容器维护替换后核验保留。 |
| A-12 | 服务 / 主机重启 | 实例 ID 和数据保留；原应运行实例恢复运行，主动停止的实例保持停止。 |
| A-13 | 故障与删除 | 注入 Docker 停止失败、不可达和目录清理失败，界面显示真实结果；完成清理后才释放配额，重试不影响其他实例。 |
| A-14 | 旧数据迁移 | 旧 admin、实例归属和数据保留；旧 expired 有恢复入口；未归属容器不被自动删除。 |
| A-15 | 禁用 / 凭据撤销 | 管理员重置密码后，旧 HTTP、WebSocket 和 WebRTC 访问失效，建议在 10 秒内完成；实例仍保留。禁用 / 启用如纳入本期，遵循相同撤销要求，重新启用后可重新登录访问。 |
| A-16 | 用户实际操作 | 用两个真实用户完成登录、建实例、重连、中文输入、文件与音频访问；权限与持久化不能只靠编译、接口返回或容器健康状态推断。 |
| A-17 | 用户列表实例统计 | 用户拥有 1 个运行实例、1 个停止实例、1 个异常实例时列表显示 3；创建占位和完整删除后数字正确刷新，与个人配额占用一致。 |
| A-18 | 管理员修改用户密码 | 不需要目标用户旧密码即可设置新密码；旧密码和旧登录失效，新密码可登录；有 / 无邮件 provider 均可完成，不改变实例数据和运行状态。 |
| A-19 | 删除无实例普通用户 | 管理员二次确认后可删除，取消确认不产生变化；旧账号和凭据不能再访问平台，同邮箱新账号不继承旧用户 ID 或权限；普通用户不能调用此功能，建议的内置管理员保护有效。 |
| A-20 | 删除有实例用户 | 用户名下含运行、停止、异常实例时，二次确认展示全量影响；账号被禁止访问，全部容器、profile、下载数据确认清理后才完成账号删除，其他用户资源不受影响。 |
| A-21 | 用户删除失败与并发恢复 | 模拟删除与创建并发、Docker 不可达、目录清理失败和 manager 重启；账号保持删除中或删除失败、禁止新操作、剩余实例数准确；重试能完成清理，不残留无主实例、不误删其他用户数据。 |

## 12. 建议默认值与本期边界

以下是为了形成完整流程而补充的建议，可在评审中修改：

| 项目 | 本草案建议 |
| --- | --- |
| “一直在” | 默认持续运行，用户可以手动停止；任何停止都不等于删除。 |
| 管理员身份 | 保留 `admin` 登录名，普通用户用邮箱；管理员邮箱可选。 |
| 邮件接入 | 首期 SMTP；配置来自部署环境，后台展示状态和测试结果。 |
| 初始密码 | 邮件模式随机生成、手工模式管理员指定；首次登录强制改密。 |
| 密码规则 / 登录期 | 最少 12 个字符；平台登录有效期 7 天，与实例寿命无关。 |
| 忘记密码 | 本期由管理员重置；自助邮件找回后续再做。 |
| 账号保护与禁用 | 用户及名下全部实例、数据一并删除和二次确认已明确；建议保护内置管理员，并纳入独立的禁用 / 启用功能。 |
| 管理员实例权限 | 删除用户时已明确具有清理该用户全部实例的权限；独立启停或删除他人单个实例尚未明确，不默认开放他人桌面或文件。 |
| 管理体验补充 | 实例命名、用户搜索筛选与分页、点击数量查看实例信息、资源概览、邮件状态与测试、管理操作记录建议纳入本期；页面可合并，避免扩大后台复杂度。 |
| 故障恢复 | 保存期望运行状态，基础设施恢复后自动恢复应运行实例；失败有界重试并保留数据。 |

本期交付范围是：部署初始化、两类固定角色、管理员用户列表（含新增、实例数量展示、配额管理、用户密码修改 / 重置、二次确认后删除用户及其全部实例与数据）、邮件 / 手工密码交付、个人登录改密、持久实例生命周期、状态核对与安全迁移。现有远程浏览器交互能力延续使用。

本期不包含公开注册、SSO / MFA、企业组织权限、用户间实例共享或转移、计费套餐、实例租期、空闲自动休眠、多节点调度、备份管理界面、录像或浏览行为审计，也不同时重设计音视频编码质量。

需求评审完成后再产出技术设计、数据库迁移方案和实施任务拆分。
